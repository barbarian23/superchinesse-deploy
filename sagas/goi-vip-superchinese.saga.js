import { call, put, takeLatest } from "redux-saga/effects";
import {
  goivipsuperchineseAction
} from "../actions";

//
const worksomething = function* (action) {
  try {
    
  } catch (e) {
    console.log("work something error", e);
  }
};

export const goivipsuperchineseWatcher = function* () {
};
